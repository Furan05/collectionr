<img src="<%= @offer.image_url %>" alt="">
<%= link_to "Edit", edit_offer_path(@offer) %>
<%# fait moi un lien vers destroy %>
<%= link_to "Destroy",
    offer_path(@offer),
    data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>


